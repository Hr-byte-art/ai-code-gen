-- 可选：为现有用户重新生成更短的分享码
-- 注意：执行前请备份数据库！

-- 如果你想为现有用户重新生成分享码，可以执行以下步骤：
-- 1. 备份数据库
-- 2. 停止应用程序
-- 3. 执行此脚本
-- 4. 重启应用程序

-- 示例：为现有用户生成新的分享码（需要根据实际需求调整）
-- UPDATE user SET shareCode = CONCAT('AI', UPPER(SUBSTRING(MD5(RAND()), 1, 6))) WHERE shareCode IS NOT NULL;

-- 或者，你可以选择保留现有用户的分享码，只对新注册用户使用新规则
-- 这样可以避免影响已经分享出去的邀请码
